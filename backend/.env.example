# .env example for the backend (apps/api)
# Copy this to .env and adjust secure values in a real setup.

# Secret key for JWT signing (use a long, random value in production)
SECRET_KEY="CHANGE_THIS_TO_A_RANDOM_SECRET"

# JWT Algorithm (default HS256)
ALGORITHM="HS256"

# Access token expiry in minutes
ACCESS_TOKEN_EXPIRE_MINUTES="15"

# Database URL (SQLite by default, change for other DBs)
DATABASE_URL="sqlite:///./naachiyars.db"
