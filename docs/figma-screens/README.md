# Figma Notes

- Authentication screens mirror the provided Section 1 layouts: branded header, centered card, gradient background, and role switcher links.
- Each role (User, Supervisor, Admin) has distinct subtitle copy while reusing the shared form component.
- Force-change-password shows a two-field form with security messaging and primary CTA.
